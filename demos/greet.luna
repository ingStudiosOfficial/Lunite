let a = false
let b = false

func greet(name) {
    if (name == "John") {
        out("Hi John!")
        return true
    } else {
        out(f"GET OUT { to_upper(name) }.")
        return false
    }
}

a = greet("John")
b = greet("Jane")

out(str(a) + " " + str(b))
